 angular.module('listrApp').factory('ListrListService', ['$http', '$q', function($http, $q) {

	const LIST_SERVICE_URI = "api/list/";
	var factory = {
		
	}
	return factory;
	
	//TO DO: List service functions (when backend is created)

 }]);